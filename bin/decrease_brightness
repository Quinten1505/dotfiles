#!/bin/bash
if [[ $(query_light) -gt 20 ]]; then
	xbacklight -dec 10
fi
#notify-no-icon "♪ $(amixer get Master | tail -1 | awk '{print $5}' | tr -d '[]')"
icon=$BRI_ICON
notify-no-icon "$icon $(query_light)"
